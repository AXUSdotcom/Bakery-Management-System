# Copy to .env for local reference (PHP reads real process env vars — see README
# for how to load this file with `php -S` locally). On Railway, set these as
# service Variables instead, pointing at the MySQL plugin's own variables.

DB_HOST=mysql.railway.internal
DB_PORT=3306
DB_NAME=sweet_bakers
DB_USER=root
DB_PASS=lxTONnPxatDerXEUKJvPqkCoYjkjJWjd

# 'production' turns on secure (HTTPS-only) session cookies — see src/Support/Auth.php
APP_ENV=local
